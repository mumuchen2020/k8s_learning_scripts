apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: exp-redis-export-nfs-pvc
  labels:
    name: exp-redis-export-nfs-pvc
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi